#include "Pods-Tests-ios-photo-viewer.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers/Build" "${PODS_ROOT}/Headers/Build/ios-photo-viewer" "${PODS_ROOT}/Headers/Public" "${PODS_ROOT}/Headers/Public/ios-photo-viewer"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}